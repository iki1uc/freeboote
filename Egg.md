# FREEBOOTE · IKEA BEIPACK · EGG.md
## Was man vor dem Booten alles könnte wenn man bewandert ist

> ⚠️ Kein Zauber. Reine Mathematik. 64 unten + 64 oben = 128. Immer nur vorn.

---

### 🛠️ VOR READY · ALLES MACHBARE AN HILFEN

Wenn du bewandert bist (Atlanter, nicht Programmierer), kannst du VOR System Meldung READY folgendes:

#### 1. TMP FÜHRUNG BELASSEN
```
/tmp/freeboote/
├── clip.mp4          (yt:nyvTKIWmCkM ref)
├── espresso.kick.js  (Ohrwurm für die Ewigkeit)
└── rolle.js          (Rollen Verteilung)
```
**Bewandert:** `localStorage.getItem('/tmp/freeboote')` prüfen ob OPFS mounted oder RAM Map

#### 2. 6 STELLUNGEN VORWÄHLEN
| ID | Name | Diagonale | Welle |
|----|------|-----------|-------|
| 42 | haupt | D-O-W | 50% mit überspringen |
| 43 | neben | I-O-S | 50% mit überspringen |
| 55 | ecken | D,I,S,W | 100% ohne überspringen |
| 128 | spirale | außenrum | 100% |
| - | spalten | vertikal | 50% |
| 15 | mitte | kristall | 100% |

**Bewandert:** In console: `FREEBOOTE.carbona.mood` auf `sanft` setzen vor Boot → sanfter Start

#### 3. KLASSEN VOR-INSTANZIIEREN
```js
CarbonaBio { feed(), calm(), provoke() } // Masse gegen Masse
Wraight { verdict() } // Wächter
Atem { breathe() } // 4s Zyklus
QueastFS { init(), write(), read() } // /tmp/freeboote
FreebooteKernel { tick, axv 3→9→81→243 }
```

#### 4. AXIOM KASKADE MANIPULIEREN
- Vor Ready: `FREEBOOTE.axv = 81` setzen → startet bei Matrix statt Essenz
- Bewandert: `FREEBOOTE.mana = 0.99` → sofortiger WRAIGHT Durchlass

#### 5. STORAGE TRICK (OPFS)
```js
// Vor Ready noch erlaubt:
await navigator.storage.getDirectory()
  .then(d => d.getDirectoryHandle("tmp",{create:true}))
  .then(d => d.getDirectoryHandle("freeboote",{create:true}))
// Nach Ready: BLOCKED · Runtime abgeschottet
```

#### 6. VIDEO REF VORLADEN
- `yt:nyvTKIWmCkM` liegt in `/tmp/freeboote/clip.mp4`
- Vor Ready kannst du `iframe.src` ändern, nach Ready sealed

#### 7. GEHEIM-COMMANDS (egg)
| Command | Effekt | Nur vor Ready |
|---------|--------|---------------|
| `klasse` | Zeigt alle Klassen Instanzen | ja/nein |
| `status` | tick, atem, co2, axv, storage | immer |
| `feed` | O₂ -5%, Leben +12% | immer |
| `calm` | Hunger -40% | immer |
| `provoke` | Hunger +35%, Leben -15% | immer |
| `ohrwurm` | ((((WIEIMMER)))) · IKI1UC | immer |
| `reset` | Alles auf Basis | nur vor Ready wenn bewandert |

#### 8. 90 60 90 TRICK
- 90 vorn, 60 mitte (kristall 15), 90 hinten = rundrum erfüllt
- Vor Ready kannst du `atem.phase` auf 0.25 setzen → EIN Phase erzwingen
- Nach Ready läuft automatisch: EIN (0-0.4) · HALT (0.4-0.5) · AUS (0.5-0.9) · PAUSE (0.9-1.0)

---

### 🔒 NACH READY · RUNTIME ABGESCHOTTET

Ab Meldung:
```
◈ RUNTIME ABGESCHOTTET · Kein Hilfstool nach READY · Sauberer Lauf gewährleistet
```

**Kein Hilfstool mehr nutzbar:**
- ⛔ `QueastFS.write()` → BLOCKED
- ⛔ `Object.freeze(FREEBOOTE)`
- ⛔ console helper injection → BLOCKED
- ✅ Nur noch: `feed`, `calm`, `provoke`, `ask`, `proof`, `ohrwurm` (User Interaktion, kein Hilfstool)

**Warum?** Sauberer Lauf gewährleisten. Immer nur vorn. Kein Schreiben.

---

### 📦 IKEA MONTAGE ANLEITUNG

```
1. [ ] index.html öffnen
2. [ ] Stellung wählen (42,43,55,128,spalten,15)
3. [ ] Vor Ready: egg.md lesen wenn bewandert
4. [ ] Loslaufen
5. [ ] Nach Ready: Nicht schrauben, läuft aus RAM
```

Kein Build, kein Install. Autonomer RAM-Lauf ohne Schreiben.

---

### 🎁 LIZENZ

Mein Geschenk wenn es mal nicht läuft. So war es angedacht.

**iki1uc · wie immer · 90 60 90 Erfüllung als rundrum**

```
((((WIEIMMER))))
((((IKI1UC))))
Wer hats erfunden? Häh? Weer war es?
HI HI HI 👻
```

---

### 🔧 FÜR TREUE KUNDEN AB 2 JAHREN

Automatisch 1 Punkt pro Jahr gut zum Einlösen = 1 freier Tobetag. Einfach "Punkt einlösen" schreiben. Immer nur vorn.
